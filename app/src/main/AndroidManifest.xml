<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Sieć -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Mikrofon: nasłuch wake-word (offline) + sesja głosowa -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <!-- Usługi w tle (foreground services) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Powiadomienia: akcje NIXI + przypomnienia -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!--
      Wyłączenie optymalizacji baterii dla NIXI (HyperOS/MIUI ubija aplikacje
      w tle). System pokaże zwykły systemowy dialog — użytkownik może odmówić.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Otwieranie aplikacji po nazwie — listowanie pakietów z etykietami (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Pełnoekranowe okno rozmowy po "Hej Nixi" nad blokadką (Android 14) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Wznawianie nasłuchu po restarcie telefonu -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".NixiApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:enableOnBackInvokedCallback="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nixi"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- Główny ekran aplikacji -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Okno rozmowy — pojawia się nad WSZYSTKIM (także nad ekranem blokady).
          Wywoływane z foreground service po wykryciu "Hej Nixi".
        -->
        <activity
            android:name=".overlay.ConversationActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Nixi.Conversation" />

        <!-- Nasłuch wake-word "Hej Nixi" — offline, foreground (mikrofon) -->
        <service
            android:name=".wake.WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Sesja głosowa z Gemini Live (mikrofon + odtwarzanie) -->
        <service
            android:name=".live.LiveSessionService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Zrzuty ekranu w trybie ręcznym (MediaProjection) -->
        <service
            android:name=".screen.ScreenCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- Czytanie powiadomień (odczyt na żądanie + ciche reguły) -->
        <service
            android:name=".notif.NixiNotificationListener"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Accessibility: tapowanie/scrollowanie w trybie ręcznym -->
        <service
            android:name=".accessibility.NixiAccessibilityService"
            android:exported="false"
            android:label="NIXI — sterowanie ekranem (tryb ręczny)"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/nixi_accessibility_config" />
        </service>

        <!-- Redirect OAuth Spotify (Authorization Code + PKCE) -->
        <activity
            android:name=".tools.SpotifyAuthActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nixi" android:host="spotify-auth" />
            </intent-filter>
        </activity>

        <!-- przypomnienia: dokładne alarmy -->
        <receiver
            android:name=".notif.ReminderReceiver"
            android:exported="false" />

        <!-- restart nasłuchu po restarcie -->
        <receiver
            android:name=".boot.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
