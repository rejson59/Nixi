<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Nixi" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/nixi_bg</item>
        <item name="android:statusBarColor">@color/nixi_bg</item>
        <item name="android:navigationBarColor">@color/nixi_bg</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:textColor">@color/nixi_text</item>
        <item name="android:colorBackground">@color/nixi_bg</item>
    </style>

    <!-- Półprzezroczysty, powyżej blokadki -->
    <style name="Theme.Nixi.Conversation" parent="Theme.Nixi">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
